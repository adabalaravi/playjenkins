apiVersion: apps/v1
kind: Deployment
metadata:
  name: azurevote-deployment
  labels:
    app: azurevote
spec:
  replicas: 3
  selector:
    matchLabels:
      app: azurevote
  template:
    metadata:
      labels:
        app: azurevote
    spec:
      containers:
      - name: azurevote
        image:  myownacr.azurecr.io/azure-vote-front:v1
        ports:
        - containerPort: 80
